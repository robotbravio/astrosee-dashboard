'use client';

import Link from 'next/link';
import { usePathname } from 'next/navigation';

const items = [
  ['Overview', '/'],
  ['Stations', '/stations'],
  ['Events', '/events'],
  ['Alerts', '/alerts'],
  ['Settings', '/settings'],
  ['Admin', '/admin']


      </nav>
    </aside>
  );
}
